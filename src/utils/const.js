const AKA_FILE = "aka.yml";
const VAR_FILE = "variables.yml";

const GIST_PATH = `${__dirname}/../../.aka/gist.json`;
const VAR_PATH = `${__dirname}/../../${VAR_FILE}`;

module.exports = {
	AKA_FILE,
	GIST_PATH,
	VAR_FILE,
	VAR_PATH,
};
